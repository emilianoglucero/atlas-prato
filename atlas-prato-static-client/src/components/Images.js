export const Images = ( {distance} ) => {

    console.log('distance in image component is' + distance );
    if (distance > 100) {

        console.log('distance es mayor que 100');
        
    }

}